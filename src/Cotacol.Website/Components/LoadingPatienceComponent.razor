<MudContainer>
    <MudProgressCircular Color="Color.Success" Indeterminate="true" Class="my-7" />
    @if(!string.IsNullOrEmpty(WaitingMessage)){
        <MudText>@WaitingMessage</MudText>
    }
</MudContainer>
@code {
    [Parameter]
    public string? WaitingMessage { get; set; }
}